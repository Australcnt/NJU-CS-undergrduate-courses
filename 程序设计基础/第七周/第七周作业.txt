【需要提交的作业（ OJ 提交 ）】

Ex1. 编程，求从n个不同的数中取r个数的所有选择的个数（n≤12）。在main函数中实现数据的输入和输出，并调用函数实现Cnr = n! / (r! (n-r)!) 的求解。

Ex2. 设计程序，求解一个m位正整数n的第k位（自右向左）数字，其中，k = m/2。例如，49是一个2位数，其第1位数字为9，89532是一个5位数，其第2位数字为3。（要求分别用函数int CountDigit(int n)和int KthDigit(int n, int k)实现n的位数m和第k位数字的求解功能，并对函数的定义、调用及声明加以注释。）

【复习】
课件；教材3.5至3.7

【预习】
教材4.1至4.2

【阅读】《编码的奥秘》第14~16章（选做）

【训练】
1. 调试课件中例2.1和2.2中的程序，掌握函数的定义与调用方法，练习函数的不同调用形式，验证：
（1）函数名是标识符的一种（观察将函数名改为“My-Factorial”后的编译结果）；
（2）return语句的功能（改变return的位置以及其后的表达式）；
（3）有、无返回值的函数在调用形式上的区别（修改程序，在MyFactorial函数中输出结果）。

2. 有同学将课件例1.3中的程序改写成以下代码，请指出其中的错误，并说明原因。
#include <stdio.h>
int MySum(int n)
{
	scanf("%d", &n);
	return n;
}
int main( )
{
	MySum(n) = (1+n)*n/2;
	printf("Sum. of integers 1-%d: %d \n", n, MySum);
	return 0;
}

3. 将菱形输出程序改为用两个或多个函数实现。

4. （选做）将日历程序改为用多个函数实现，并添加用按键增减月份、日历自动更新的功能。（说明：开发环境一般会提供库函数getche或_getche用来捕获光标键，第一个返回值为224时说明捕获到一个功能键，进一步判断其第二个返回值为72/80时对应向上/下光标键。调用前一般需加#include <stdlib.h>或#include <conio.h>）