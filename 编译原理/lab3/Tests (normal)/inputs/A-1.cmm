int main() {
  int result = 0;
  int i = 9, j = 32, k = 57;
  i = i * (i * j - k);
  write(i);
  j = i - (j + k * (k - 1)) * (-1);
  write(j);
  k = j - (k + k * j + 1 * (i + j + k)) / (i / 2);
  write(k);
  result = i + j - k;
  write(result);
  return 0;
}
