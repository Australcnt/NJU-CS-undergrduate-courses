int main() {
  int x;
  int y;
  int z;
  x = read();
  y = read();
  z = read();
  if (x > 0 && y > 0) {
    write(1);
  } else {
    if (x == 0 && y == 0) {
      write(2);
    } else {
      if (x < 0 && y < 0) {
        write(3);
      } else {
        if (x >= y) {
          write(4);
        } else {
          if (x < z && z < y) {
            write(5);
          } else {
            write(6);
          }
        }
      }
    }
  }
  return 0;
}
